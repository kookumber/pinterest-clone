const dropHandler = (event) => {
    
    event.preventDefault(event)
    
    let eventDataTransferItems = event.dataTransfer.items

    if (eventDataTransferItems) {
        for (let i = 0; i < eventDataTransferItems.length; i++ ) {
            
        }
    }


}